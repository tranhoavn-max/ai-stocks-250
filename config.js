/* Supabase public config. Both values are PUBLIC-SAFE: the project URL and the
   publishable key are designed to ship in the browser — Row Level Security (see
   docs/specs/deployment/supabase-schema.sql) is what protects per-user data. The
   service/secret key is NEVER placed here. */
window.SUPABASE_URL = "https://sjvjhelpwumssgfcwpew.supabase.co";
window.SUPABASE_KEY = "sb_publishable_p3r-24DuCNbCYqy0ilynVQ_ZtAHVcgv";
